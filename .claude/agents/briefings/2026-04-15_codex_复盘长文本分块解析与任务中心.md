# 2026-04-15 Codex 工作简报：复盘长文本分块解析与任务中心

## 完成内容

- 为交易复盘长文字识别新增了“后台解析任务”列表接口：
  - `GET /api/v1/trades/uploads/manual/parse-text-tasks`
  - 返回最近任务的状态、摘要、日期块数、记录数、分块数和验收状态
- 复盘页新增了“后台解析任务”中心：
  - 可刷新最近任务
  - 可重新打开已完成的长文字识别结果
- 超长分组长文本现在会按日期块自动分块解析后再合并结果：
  - 默认每块最多约 `4` 个日期块或 `24` 个代码
  - 结果和验收摘要会明确显示 `chunk_count`
- 长文字识别摘要现在会额外显示后台分块信息，便于判断长任务是否正常执行

## 关键文件

- `apps/api/src/quant_platform_api/main.py`
- `apps/api/src/quant_platform_api/services.py`
- `apps/api/src/quant_platform_api/static/replay.html`
- `apps/api/src/quant_platform_api/static/replay.js`
- `tests/test_quant_platform_api.py`
- `tests/test_static_assets.py`
- `产品设计/接力模块/交易复盘模块.md`
- `产品设计/AI量化交易平台_新环境恢复与Agent接力说明.md`

## 验证结果

- `python3 tools/validate_product_design_docs.py` 通过
- `tests/test_static_assets.py` 通过
- 定向接口测试通过：
  - `test_manual_text_parse_large_grouped_text_keeps_records_when_market_fill_is_skipped`
  - `test_manual_text_parse_task_list_returns_recent_items`

## 当前真实边界

- 分块解析当前仍是“分组后串行合并”，不是多进程并行解析
- 分块规则目前是固定经验值，不是动态最优切分
- 后台解析任务中心当前只覆盖长文字识别，还没扩到截图 OCR

## 下一步建议

- 给长文字识别结果补更细的“分块进度 / 当前处理到第几块”
- 继续把截图 OCR 也接入同一套复盘任务中心
- 后续用真实长文本样本继续扩回归集，验证分块后规则识别不退化
