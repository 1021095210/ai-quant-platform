# 工作简报：复盘分钟级入场与止盈接入

- **Agent**: codex
- **日期**: 2026-04-09 08:27
- **涉及模块**: 交易复盘、分钟级真实重放、接力文档、测试

## 做了什么（What）

- 修改了 `apps/api/src/quant_platform_api/services.py`
  - 在 A 股复盘目标版本的真实重放链路上，把分钟级能力从“只支持止损触发”继续扩展到：
    - 分钟级入场时间与入场价格对齐
    - 分钟级止盈触发
  - 新增：
    - `_find_replay_intraday_entry`
    - `_find_replay_intraday_threshold_exit`
  - 当前当真实 `1min` 数据可用时：
    - 入场会优先对齐到分钟线
    - 出场会优先按分钟线判断止损/止盈是否触发
  - 当同一根分钟 K 同时触发止盈与止损时，当前按保守口径优先记为止损触发。
- 修改了 `tests/test_quant_platform_api.py`
  - 保留原有日线真实重放测试
  - 保留分钟级止损测试
  - 新增分钟级入场与止盈测试，验证：
    - 入场价格会按分钟线对齐
    - 出场会生成 `take_profit_intraday_minute`
- 更新了接力文档：
  - `产品设计/接力模块/交易复盘模块.md`
  - `产品设计/AI量化交易平台_新环境恢复与Agent接力说明.md`
  - 明确当前分钟级真实重放第一阶段已覆盖“入场对齐 + 止损/止盈触发”，但还不是完整分钟级入场/出场重放引擎。

## 为什么这样做（Why）

- 上一轮只把分钟级真实重放推进到“分钟级止损触发”，分钟线仍没有真正进入更完整的入场/出场链路。
- 这次优先补：
  - 分钟级入场对齐
  - 分钟级止盈触发
  是因为这两项最直接影响真实成交还原度，也最容易改变用户对结果真实性的判断。
- 对“同一根分钟 K 同时触发止盈和止损”的情况，当前采用保守处理，是为了避免在没有 tick 序列时伪造分钟内先后顺序。

## 结果（Result）

- 测试结果：
  - `test_replay_market_rerun_uses_real_daily_bars_when_available` 通过
  - `test_replay_market_rerun_uses_real_minute_window_for_stop_exit` 通过
  - `test_replay_market_rerun_uses_real_minute_entry_and_take_profit` 通过
  - `test_replay_js_has_valid_module_syntax` 通过
  - `tools/validate_product_design_docs.py` 通过
- 当前效果：
  - A 股目标版本在存在真实 `1min` 数据时，分钟线已经开始参与：
    - 入场价格/时间对齐
    - 止损触发
    - 止盈触发
- 已知限制：
  - 还没有做完整的分钟级入场/出场重放引擎。
  - 还没有把分钟级参数搜索真正接进优化目标版本。
  - 当前对分钟内止盈/止损冲突采用保守口径，后续若有更细粒度数据再升级。
- 建议后续 agent：
  - 继续把分钟级重放扩展到更完整的出场链路。
  - 让分钟级参数搜索真正进入目标版本评分和优化结果选择。
