# 工作简报：金融助手模块第一阶段

- **Agent**: codex
- **日期**: 2026-04-03 08:12
- **涉及模块**: `apps/api/src/quant_platform_api/models.py`, `apps/api/src/quant_platform_api/main.py`, `apps/api/src/quant_platform_api/services.py`, `apps/api/src/quant_platform_api/static/assistant.html`, `apps/api/src/quant_platform_api/static/assistant.js`, `apps/api/src/quant_platform_api/static/dashboard.html`, `apps/api/src/quant_platform_api/static/workspace.html`, `apps/api/src/quant_platform_api/static/workspace.js`, `apps/api/src/quant_platform_api/static/strategy.html`, `apps/api/src/quant_platform_api/static/indicators.html`, `apps/api/src/quant_platform_api/static/rules.html`, `apps/api/src/quant_platform_api/static/mentor.html`, `apps/api/src/quant_platform_api/static/backtests.html`, `apps/api/src/quant_platform_api/static/replay.html`, `apps/api/src/quant_platform_api/static/admin.html`, `apps/api/src/quant_platform_api/static/theme.css`, `tests/test_quant_platform_api.py`, `tests/test_static_assets.py`, `产品设计/接力模块/README.md`, `产品设计/接力模块/金融助手模块.md`, `产品设计/接力模块/首页模块.md`, `产品设计/接力模块/用户工作台模块.md`, `产品设计/AI量化交易平台_新环境恢复与Agent接力说明.md`, `.claude/agents/ACTIVE_TASKS.md`

## 做了什么（What）

- 新增独立“金融助手”模块，路由为 `/assistant`
- 新增 `AssistantResearchRequest` 和 `FinancialAssistantService`
- 金融助手第一阶段已提供：
  - 研究工作流列表：
    - 市场地图
    - 公司深研
    - 事件冲击
    - 多空辩论
    - 风险委员会
    - 投委会纪要
  - 专家编组列表：
    - 宏观与新闻台
    - 基本面研究员
    - 技术与成交结构台
    - 情绪与风格台
    - 多头研究员
    - 空头研究员
    - 风控与组合经理
  - 结构化研究输出：
    - 执行摘要
    - 专家拆解
    - 多空分歧
    - 风险核对
    - 本轮交付物
    - 建议下一步
    - 相关模块入口
- 金融助手支持继续细化研究，不是一次性静态结果
- 已接入真实 LLM 链路：
  - 复用 OpenAI 兼容网关
  - 返回模式显示为 `AI 研究编组`
  - 未配置时回退为 `平台研究模板`
- 首页、用户工作台和各主要研究页面导航都已加入金融助手入口
- 同步新增并更新了：
  - 模块接力文档
  - 首页模块文档
  - 用户工作台模块文档
  - 主接力文档

## 为什么这样做（Why）

- 用户给出的本地“金融助手”截图和 `TradingAgents` 参考都指向同一个方向：需要的不是第二个教学页，而是更偏机构研究协作台的模块
- 因此这轮没有把它做成“问答机器人”，而是按“工作流 + 专家编组 + 结构化输出”的方式落第一阶段
- 金融导师和金融助手现在职责分开：
  - 金融导师负责概念、指标和平台使用解释
  - 金融助手负责组织研究任务、生成多视角研究结果
- 先做成结构化研究基线，可以让后续 agent 更容易接入页面上下文、真实数据引用和导出能力

## 结果（Result）

- 全量测试通过：
  - `python3 -m unittest discover -s tests -p 'test_*.py'`
  - `134` 个测试 `OK`
- 新增测试覆盖：
  - 助手页面登录后可访问
  - 助手工作流和结构化分析接口
  - 助手 LLM 模式
  - 助手静态资源结构
  - `/assistant` 登录保护
- 运行验证通过：
  - 本地 `/api/v1/assistant/workflows` 返回 6 个工作流和 7 个专家编组
  - 本地 `/api/v1/assistant/analyze` 已确认返回 `answer_source=llm`
  - 外链登录后 `/assistant` 页面已可访问，并确认引用新版 `assistant.js?v=20260403a`
  - 外链分析请求已成功返回 `answer_source=llm`
- 已知限制：
  - 当前金融助手仍是“结构化研究助手第一阶段”，还没有接入当前策略、回测、复盘页面的上下文数据
  - 还未提供导出为正式研究纪要或投委会文档的能力
  - 外链仍是临时隧道，不是正式长期部署
