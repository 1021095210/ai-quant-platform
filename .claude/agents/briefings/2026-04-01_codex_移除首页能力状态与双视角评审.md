# 工作简报：移除首页能力状态与双视角评审

- **Agent**: codex
- **日期**: 2026-04-01 09:50 UTC
- **涉及模块**: 首页静态页、样式与测试、接力文档、产品设计评审文档、Git 同步流程

## 做了什么（What）

- 删除首页 `dashboard.html` 中的“能力状态”四卡区块，使首页仅作为平台入口页，不承载能力矩阵展示。
- 同步清理样式与测试中的相关引用，保证全量测试通过。
- 更新接力文档，明确首页定位与当前 GitHub 远端基线描述（以 GitHub `main` HEAD 为准）。
- 新增两份独立评审文档，分别从“交易员视角”和“平台设计视角”给出按优先级排序的可落地建议，便于后续讨论与迭代，不覆盖现有版本。
- 处理本地与远端 `main` 因历史 API 同步造成的提交 SHA 分叉：从 `origin/main` 新建分支并 cherry-pick 小改动后再 fast-forward 推送，避免远端历史混乱。

## 为什么这样做（Why）

- 用户明确要求移除首页“能力状态”，并将能力边界表达移动到登录后的研究流程中（更符合成熟平台的入口页定位）。
- “能力矩阵”这类信息更适合放在工作台/帮助页，并与真实执行能力绑定，避免首页对能力造成过度承诺。
- 通过从 `origin/main` 叠加改动并推送，保证后续接力以 GitHub 为单一事实来源，降低新 agent 的恢复成本。

## 结果（Result）

- 测试：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过（53 tests OK）。
- 已知限制：
  - 本地仍可能存在与远端不同 SHA 的历史提交（源于早期 API 同步方式），但后续以 GitHub `main` 为准即可。
  - `quant_platform.db` 为运行态产物，未纳入提交。
- 后续建议：
  - 将“能力边界/能力矩阵”迁移到工作台或帮助页，并与回测结果强制展示的假设/数据快照信息形成闭环。
  - 先按评审文档的 P0 项补齐“回测配置可配置化、数据追踪、最小风控与可复现对比”，再扩多市场执行层。

