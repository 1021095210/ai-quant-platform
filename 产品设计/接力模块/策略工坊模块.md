# 策略工坊模块

## 当前定位

- 路由：`/strategy`
- 角色：把自然语言交易想法转成结构化策略与 Python 策略骨架

## 当前已实现

- 支持 A股 / 美股 / 加密货币 / 伦敦金
- 支持混合周期：
  - 1分钟 / 5分钟 / 15分钟 / 30分钟 / 1小时 / 4小时 / 日线 / 周线 / 月线
- 已新增“当前能力声明”区块：
  - 会按当前市场与周期显示“可描述 / 可真实回测 / 数据链路”状态
  - A股日线单周期会显示“可直接运行真实日线回测”
  - A股混合周期 / 非日线会显示“仅支持日线兼容层回测”
  - 美股 / 加密货币 / 伦敦金会明确显示“当前暂不开放真实回测”
- 已新增“市场支持状态”矩阵：
  - 明确区分“可生成与保存策略语义”和“可真实执行回测”
- 顶部支持直接编辑：
  - 策略名称
  - 版本标签
- 已新增“系统理解结果”与“待补充与风险提示”区块：
  - 会先展示系统理解到的：
    - 市场
    - 周期
    - 入场条件
    - 离场条件
    - 数据依赖
    - 执行假设
  - 会单独展示：
    - 待补充问题
    - 不支持项
    - 未来函数风险
- 已新增策略生成状态体系：
  - `ready`
  - `semantic_only`
  - `needs_confirmation`
  - `rejected`
- `保存项目` 和 `去回测中心` 当前不再只看“有没有生成代码”，还会受生成状态控制：
  - `ready` 可保存并继续
  - `semantic_only` 可保存，但真实执行受平台边界限制
  - `needs_confirmation` 不允许直接保存正式版本
  - `rejected` 不允许继续保存 / 回测
- 成功生成 Python 策略后：
  - 只有满足生成状态门槛时，后续动作按钮才会点亮
- 已打通“待补充问题 -> 二次确认 -> 重新理解”第一阶段：
  - 用户可直接在页面里填写每条待补充问题的补充说明
  - 可点击“应用补充并重新理解”重新进入生成链路
  - 当前已支持把补充说明写回：
    - 量能阈值
    - 不追高定义
    - 确认规则
    - 市场环境条件
    - 仓位规则
  - 当前已升级为“多轮澄清流”第一阶段：
    - 页面会区分：
      - 已确认的补充项
      - 当前仍待补充的问题
    - 如果上一轮补充后仍然不够具体，系统会继续追问，而不是把所有问题都当成全新问题重新开始
    - 当前已支持的继续追问包括：
      - 量能阈值仍缺数值口径
      - 追高限制仍缺边界
      - 市场环境仍缺判定标准
      - 分批建仓仍缺加仓触发条件
- 已新增“自然语言 -> 结构化规格 -> 平台硬校验”并排预览第一阶段：
  - 页面会单独展示：
    - 自然语言输入
    - 结构化规格
    - 平台硬校验清单
  - 结构化规格会明确展示：
    - 市场
    - 周期
    - 规则数量
    - 仓位
    - 执行假设
    - 待补充项
  - 平台硬校验当前会明确区分：
    - 市场与回测支持范围
    - 周期与执行兼容范围
    - 模糊条件完整性
    - 未来函数风险
    - 平台不支持的数据依赖
    - 当前是否具备正式策略生成资格
- 已新增“生成链路对照”视图：
  - 会把策略生成拆成：
    - 自然语言策略想法
    - AI 候选理解
    - 结构化策略规格
    - 平台硬校验
    - DSL / 结构化执行规格
    - Python 策略代码
  - 用途是让用户清楚知道：
    - 哪一层是真值层
    - 哪一层是平台规则判断
    - 哪一层只是派生代码结果
- 策略工坊当前已接入“AI 候选理解层”：
  - 当前链路变成：
    - 自然语言输入
    - AI 候选理解
    - 平台结构化规格
    - 平台硬校验
    - 用户确认 / 澄清
    - 再生成 DSL / Python
  - AI 当前只负责：
    - 补强长中文策略表达的语义理解
    - 补充待确认项
    - 输出候选摘要和语义风险提醒
  - AI 当前不能：
    - 直接决定平台真值层
    - 跳过平台硬校验
    - 直接自由写代码并作为最终策略真值
  - 页面当前会直接展示：
    - 理解模式
    - AI 候选摘要
    - AI 待确认项
    - AI 风险提示
  - 当前 AI 候选理解已进一步映射进结构化规格：
    - AI 市场理解
    - AI 周期提示
    - AI 数据依赖
    - AI 入场 / 过滤 / 离场 / 风控 / 仓位意图
    - AI 执行假设
    - AI 建议优先核对的 DSL 字段
    - AI 建议优先确认的值域
  - 当前 AI 待确认项会优先归并到平台已有澄清问题体系：
    - 量能阈值
    - 追高限制
    - 确认规则
    - 市场环境
    - 仓位规则
  - 这样做的目的不是让 AI 决定真值，而是让 AI 结果更深进入“结构化规格 + 待确认流”，便于后续澄清和硬校验
  - 当前 AI 结构提示已继续映射进更细的真值层：
    - `filters`
    - `execution_timing`
  - 当前页面的字段级对照解释会继续显示：
    - 过滤规则对应的 DSL / Python 片段
    - 触发可见性与执行时序对应的 DSL / Python 片段
- 当前已新增 `LLM 模型选择`：
  - 页面会显示 `模型选择` 下拉框
  - 当前可选项来自统一接口：`/api/v1/platform/llm-profiles`
  - 当前支持的选择类型：
    - 模块默认模型
    - 策略模型
    - 导师模型
    - 研究模型
    - DeepSeek
    - 火山方舟
  - 当前火山方舟默认候选模型约定为：
    - `deepseek-v3-2-251201`
  - 当前 AI 只在“候选理解层”使用所选模型：
    - 不改变平台硬校验
  - 当前不支持的数据依赖拦截已继续扩到：
    - 北向资金 / 主力资金 / 资金净流入
    - 龙虎榜 / 融资融券 / 两融
    - 封单 / 封板资金
    - 不改变最终 DSL / Python 的真值资格
    - 如果显式选择的模型当前不可用，会诚实回退到模块默认模型
- 支持策略项目保存和版本管理

## 主要文件

- `apps/api/src/quant_platform_api/static/strategy.html`
- `apps/api/src/quant_platform_api/static/strategy.js`
- `apps/api/src/quant_platform_api/main.py`
- `apps/api/src/quant_platform_api/services.py`

## 当前设计约束

- 用户可编辑的是“策略名称”和“版本标签”，不是系统内部 `version_id`
- 上一步完成后，下一步按钮必须显式点亮
- 混合周期 DSL 可以先保留语义层，即使执行层仍按 `backtest_timeframe`
- 多市场与多周期能力必须先声明边界，不能继续让用户误以为所有市场都已开放真实回测
- 策略工坊后续必须坚持：
  - 先“自然语言 -> 结构化策略规格”
  - 再做平台硬校验和用户确认
  - 最后才生成 DSL / Python
- 模糊条件、缺参数、未来函数风险和平台不支持项，不允许被系统悄悄补全后直接生成最终策略
- 当前“结构化规格”和“平台硬校验”是真值与规则层，不能让 AI 直接覆盖或跳过
- 当前模型选择是“理解层配置”，不是“真值层配置”：
  - 用户可切换理解使用的模型
  - 但平台结构化规格、硬校验和生成资格仍由平台规则决定
  - 当前“生成链路对照”已经明确：
  - 结构化规格是真值层
  - 平台硬校验是规则层
  - Python 代码只是派生层，不应反向作为真值来源
- 当前多轮澄清已进入“上下文记忆”阶段：
  - 已确认补充项会继续带入下一轮
  - 页面会显示：
    - 当前重点追问
    - 已确认补充项摘要
    - 哪些继续追问来自上一轮哪个回答
- 已新增“字段级对照解释”视图：
  - 会把用户表达映射到：
    - 结构化规格字段
    - DSL 路径
    - Python 代码中的职责位置
    - DSL 片段
    - Python 片段
  - 当前已覆盖：
    - 市场范围
    - 主执行周期
    - 入场规则
    - 观察层上下文
    - 离场与风控
    - 仓位规则
    - 正式生成门槛
- 未来函数风险规则当前已继续扩展：
  - 不只拦“当日最高价 / 最低价”
  - 也会继续拦：
    - 当日收盘价 / 今日收盘价直接参与当日盘中决策
    - 收盘前使用收盘价确认
    - 当日涨幅 / 今日涨幅 / 最终涨幅直接参与当日盘中决策
    - 当日振幅 / 今日振幅 / 最终振幅直接参与当日盘中决策
    - 当日成交量 / 全天成交量 / 最终成交量直接参与盘中确认或开平仓
    - 当日成交额 / 全天成交额 / 最终成交额直接参与盘中确认或开平仓
    - 当日换手率 / 全天换手率 / 最终换手率直接参与盘中确认或开平仓
  - 当前也开始显式区分“安全的收盘后确认 + 次日执行”：
    - 如果策略明确写成“收盘后确认，次日开盘/次日执行”，平台会把它写进 `execution_timing`
    - 这类表达不会再被一律误判成未来函数
- 平台不支持的数据依赖当前已继续扩展：
  - 当前会明确拦截：
    - 公告 / 新闻 / 研报 / 舆情 / 消息面驱动的真值策略条件
    - 集合竞价 / 盘前盘后 / 夜盘执行语义
  - 这些表达当前不会被偷偷翻译成可执行策略，而是会进入硬校验拒绝项

## 下一步关注

- 更强的版本说明与保存提示
- 更真实的多周期执行层
- 优先阅读：
  - `产品设计/模块AI设计/策略工坊模块_AI设计.md`
  - `产品设计/模块AI设计/策略工坊模块_详细查缺补漏.md`
- 当前最缺：
  - 多轮澄清流当前仍缺更连续的对话式追问，但已回答项、AI 待确认项和继续追问的上下文记忆已经接入
  - AI 候选理解当前已进入结构化规格提示层，但还没有全面驱动每个 DSL 字段的确认和补全
  - “自然语言 -> 结构化规格 -> DSL/Python”更完整的并排视图
  - 更系统的未来函数风险规则库
  - 结构化规格到 DSL / Python 的逐字段映射解释
